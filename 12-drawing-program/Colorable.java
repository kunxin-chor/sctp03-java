public interface Colorable {
    // when a class implements an interface
    // that class must have ALL the methods
    // from the interface
    void setColor(String color);
    String getColor();
}
